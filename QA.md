# Проверка 23.09.2026

## Автоматически

- `node --check server.mjs` — успешно.
- `node --check app.js` — успешно.
- `node --test tests/server.test.mjs` — успешно. Изолированный HTTP-сервер: страница, запрет доступа к служебным файлам и заявкам, неверный Origin, некорректные данные и согласие, параллельные дубликаты, сохранение одной записи, конфликт UUID.

## Браузер

Проверено во встроенном Chromium. Размеры CSS viewport: 320, 360, 390, 430, 768, 1024, 1440 px. `scrollWidth` равен `clientWidth` на всех размерах после исправления длинного заголовка на 320 px. Горизонтального переполнения страницы нет.

- Визуально проверены первый экран desktop и mobile, мобильное меню, карточки, форма, FAQ и нижняя часть.
- Меню открывается, переход к разделу закрывает его.
- Кнопки записи открывают модальное окно; закрытие возвращает фокус.
- Ошибочный телефон показывает понятное сообщение.
- Контрольная заявка с вымышленными данными сохранена **локально**; подтверждение явно сообщает об отсутствии передачи в клинику.
- FAQ раскрывает текст ответа.
- Ошибок в журнале браузера на момент проверки не обнаружено.
- Контакты и фотографии сверены с действующим сайтом.

## Не проверено

Доставка в реальную CRM/администратору, настоящие звонки, Safari/iOS и Android на физических устройствах, Lighthouse/полевые Core Web Vitals, рекламная конверсия. Сайт не опубликован. Счётчик Метрики в локальной версии не активирован.

## Дополнение: панель и публикация

`node --test tests/server.test.mjs tests/admin.test.mjs` — 2 теста успешно. Проверены вход, неверный пароль, запрет чужого Origin, HttpOnly/SameSite, CSRF, редактирование, история, конфликт версий, запрет HTTP-доступа к файлу заявки, выход и блокировка старой сессии. `node scripts/build-pages.mjs` — успешно, в статической версии сбор заявок отключён.
